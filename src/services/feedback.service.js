import api from "../api/api.config";

const FeedbackService = {
  get: async (productId, handleSuccess, handleFailed) => {},
  add: async (newFeedback, handleSuccess, handleFailed) => {},
  edit: async (newFeedback, feedbackId, handleSuccess, handleFailed) => {},
  delete: async (feedbackId, handleSuccess, handleFailed) => {}
};

export default FeedbackService;
