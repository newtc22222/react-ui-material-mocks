import api from "../api/api.config";

const UserService = {
  getCurrentUser: async (handleSuccess, handleFailed) => {},
  changeInformation: async (newInformation, handleSuccess, handleFailed) => {},
  changePassword: async (newPassword, handleSuccess, handleFailed) => {},
  disableAccount: async (handleSuccess, handleFailed) => {}
};

export default UserService;
