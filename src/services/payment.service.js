import api from "../api/api.config";

const PaymentService = {
  payWithMomo: async (momoObject, handleSuccess, handleFailed) => {},
  payWithPaypal: async (paypalObject, handleSuccess, handleFailed) => {}
};

export default PaymentService;
