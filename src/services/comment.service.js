import api from "../api/api.config";

const CommentService = {
  get: async (productId, handleSuccess, handleFailed) => {},
  add: async (newComment, handleSuccess, handleFailed) => {},
  edit: async (newComment, commentId, handleSuccess, handleFailed) => {},
  delete: async (commentId, handleSuccess, handleFailed) => {}
};

export default CommentService;
